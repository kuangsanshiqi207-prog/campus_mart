<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.admin.AdminMapper">

    <sql id="BaseColumns">
        id, username, password, nickname, avatar, role, status,
        create_time, update_time, deleted
    </sql>

    <select id="getByUsername" resultType="com.example.entity.Admin">
        SELECT <include refid="BaseColumns"/>
        FROM admin
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="getById" resultType="com.example.entity.Admin">
        SELECT <include refid="BaseColumns"/>
        FROM admin
        WHERE id = #{id} AND deleted = 0
    </select>

</mapper>
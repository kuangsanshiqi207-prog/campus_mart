<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.user.UserMapper">

    <sql id="BaseColumns">
        id, username, password, nickname, avatar, phone, email,
        role, status, certified, credit_score, create_time, update_time, deleted
    </sql>

    <select id="getByUsername" resultType="com.example.entity.User">
        SELECT <include refid="BaseColumns"/>
        FROM user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="getByPhone" resultType="com.example.entity.User">
        SELECT <include refid="BaseColumns"/>
        FROM user
        WHERE phone = #{phone} AND deleted = 0
    </select>

    <select id="getById" resultType="com.example.entity.User">
        SELECT <include refid="BaseColumns"/>
        FROM user
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="com.example.entity.User">
        INSERT INTO user (
            id, username, password, nickname, avatar, phone, email,
            role, status, certified, credit_score, create_time, update_time, deleted
        ) VALUES (
                     #{id}, #{username}, #{password}, #{nickname}, #{avatar}, #{phone}, #{email},
                     #{role}, #{status}, #{certified}, #{creditScore}, #{createTime}, #{updateTime}, 0
                 )
    </insert>

    <update id="updatePassword">
        UPDATE user
        SET password = #{password}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateProfile" parameterType="com.example.entity.User">
        UPDATE user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="email != null">email = #{email},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateCertified">
        UPDATE user
        SET certified = #{certified}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <sql id="AdminQueryCondition">
        <where>
            deleted = 0
            <if test="query.keyword != null and query.keyword != ''">
                AND (
                username LIKE CONCAT('%', #{query.keyword}, '%')
                OR nickname LIKE CONCAT('%', #{query.keyword}, '%')
                OR phone LIKE CONCAT('%', #{query.keyword}, '%')
                )
            </if>
            <if test="query.status != null and query.status != ''">
                AND status = #{query.status}
            </if>
            <if test="query.certified != null">
                AND certified = #{query.certified}
            </if>
        </where>
    </sql>

    <select id="listUsers" resultType="com.example.entity.User">
        SELECT id, username, password, nickname, avatar, phone, email,
        role, status, certified, credit_score, create_time, update_time, deleted
        FROM user
        <include refid="AdminQueryCondition"/>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countUsers" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM user
        <include refid="AdminQueryCondition"/>
    </select>

    <update id="updateStatus">
        UPDATE user SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateCreditScore">
        UPDATE user SET credit_score = #{score}, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.chat.ChatMessageMapper">

    <sql id="BaseColumns">
        id, conversation_id, from_user_id, to_user_id,
        type, content, status, create_time
    </sql>

    <insert id="insert" parameterType="com.example.entity.ChatMessage">
        INSERT INTO chat_message (
            id, conversation_id, from_user_id, to_user_id,
            type, content, status, create_time
        ) VALUES (
                     #{id}, #{conversationId}, #{fromUserId}, #{toUserId},
                     #{type}, #{content}, #{status}, #{createTime}
                 )
    </insert>

    <!-- 游标分页：按 ID 倒序，取比 lastId 小的 -->
    <select id="listByConversation" resultType="com.example.entity.ChatMessage">
        SELECT <include refid="BaseColumns"/>
        FROM chat_message
        WHERE conversation_id = #{conversationId}
        <if test="lastId != null">
            AND id &lt; #{lastId}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <update id="markRead">
        UPDATE chat_message
        SET status = 'read'
        WHERE conversation_id = #{conversationId}
          AND to_user_id = #{toUserId}
          AND status != 'read'
    </update>

</mapper>
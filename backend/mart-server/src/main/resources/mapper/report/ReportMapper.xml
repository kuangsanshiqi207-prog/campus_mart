<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.report.ReportMapper">

    <resultMap id="ReportVOMap" type="com.example.vo.report.ReportVO">
        <id property="id" column="id"/>
        <result property="reporterId" column="reporter_id"/>
        <result property="reporterNickname" column="reporter_nickname"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="reason" column="reason"/>
        <result property="description" column="description"/>
        <result property="images" column="images"
                typeHandler="com.example.handle.CommaSeparatedStringListTypeHandler"/>
        <result property="status" column="status"/>
        <result property="action" column="action"/>
        <result property="handleReason" column="handle_reason"/>
        <result property="handleTime" column="handle_time"/>
        <result property="createTime" column="create_time"/>
        <result property="hasAppeal" column="has_appeal"/>
    </resultMap>

    <sql id="BaseColumns">
        id, reporter_id, target_type, target_id, reason, description,
        images, status, action, handle_reason, handler_id, handle_time,
        create_time, deleted
    </sql>

    <sql id="VOColumns">
        r.id,
        r.reporter_id,
        u.nickname AS reporter_nickname,
        r.target_type,
        r.target_id,
        r.reason,
        r.description,
        r.images,
        r.status,
        r.action,
        r.handle_reason,
        r.handle_time,
        r.create_time,
        (SELECT COUNT(*) FROM appeal a WHERE a.report_id = r.id AND a.deleted = 0) AS has_appeal
    </sql>

    <select id="getById" resultType="com.example.entity.Report">
        SELECT <include refid="BaseColumns"/>
        FROM report
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="getByReporterAndTarget" resultType="com.example.entity.Report">
        SELECT <include refid="BaseColumns"/>
        FROM report
        WHERE reporter_id = #{reporterId}
        AND target_type = #{targetType}
        AND target_id = #{targetId}
        AND deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.example.entity.Report">
        INSERT INTO report (
            id, reporter_id, target_type, target_id, reason, description,
            images, status, create_time, deleted
        ) VALUES (
                     #{id}, #{reporterId}, #{targetType}, #{targetId}, #{reason}, #{description},
                     #{images}, #{status}, #{createTime}, 0
                 )
    </insert>

    <select id="getReportVOById" resultMap="ReportVOMap">
        SELECT <include refid="VOColumns"/>
        FROM report r
        LEFT JOIN user u ON r.reporter_id = u.id
        WHERE r.id = #{id} AND r.deleted = 0
    </select>

    <select id="listByReporter" resultMap="ReportVOMap">
        SELECT <include refid="VOColumns"/>
        FROM report r
        LEFT JOIN user u ON r.reporter_id = u.id
        WHERE r.reporter_id = #{reporterId} AND r.deleted = 0
        ORDER BY r.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByReporter" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM report
        WHERE reporter_id = #{reporterId} AND deleted = 0
    </select>

</mapper>
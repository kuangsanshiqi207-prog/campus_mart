<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.stats.CreditLogMapper">

    <sql id="BaseColumns">
        id, user_id, delta, before_score, after_score,
        reason, biz_type, biz_id, create_time
    </sql>

    <insert id="insert" parameterType="com.example.entity.CreditLog">
        INSERT INTO credit_log (
            id, user_id, delta, before_score, after_score,
            reason, biz_type, biz_id, create_time
        ) VALUES (
                     #{id}, #{userId}, #{delta}, #{beforeScore}, #{afterScore},
                     #{reason}, #{bizType}, #{bizId}, #{createTime}
                 )
    </insert>

    <select id="listByUserId" resultType="com.example.entity.CreditLog">
        SELECT <include refid="BaseColumns"/>
        FROM credit_log
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM credit_log
        WHERE user_id = #{userId}
    </select>

</mapper>